Amarin Hutt
Paul Robin

Projet de SD

pour lancer utiliser "python3 launch.py"
/!\ avant de lancer vérifier que rpyc est bien installé
sinon utilisé pip3 pour l'installer (mode local si vous n'avez pas les droits)


Notes diverses:

Les classes List() et Dict() sont threads safes dans le sens où la 
machine virtuel python gère l'accès concurrent de ces objets (bien que
le objet contenu ne le sont pas).
ex : liste.remove(x) est thread safe alors que liste[0]=x ne l'est pas.
